<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/harvard_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/harvard_background"
        android:scaleType="fitXY"
        android:adjustViewBounds="true" />

    <include
        layout="@layout/header_layout"/>

    <Button
        android:id="@+id/buttonBarilleThomas"
        android:text="BARILLE Thomas"
        android:layout_width="150dp"
        android:layout_height="70dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="@drawable/rounded_button"/>

    <Button
        android:id="@+id/buttonCasMarcOlivier"
        android:layout_below="@id/buttonBarilleThomas"
        android:layout_width="150dp"
        android:layout_height="70dp"
        android:layout_marginTop="64dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:background="@drawable/rounded_button"
        android:text="CAS Marc-Olivier"/>

</RelativeLayout>